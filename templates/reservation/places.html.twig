{% extends 'template.html.twig' %}

{% block title %}Places index{% endblock %}
{% block PageTitle %}
    <h1>Places index</h1>
{% endblock %}
{% block body %}

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Parking_id</th>
                <th>Type</th>
                <th>Couverture</th>
            </tr>
        </thead>
        <tbody>
        {% for payment in payments %}
            <tr>
                <td>{{ payment.id }}</td>
                <td>{{ payment.montantpaye }}</td>
                <td>{{ payment.datepaiement ? payment.datepaiement|date('Y-m-d') : '' }}</td>
                <td>{{ payment.modepaiement }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}